<div class="dialog" role="dialog">
    <h1>{{ title }}</h1>
    <p>{{ message }}</p>
    <div class="code">{{ additionalInfo }}</div>
    <button id="abort">{{i18n "dialog.button.close"}}</button>
</div>
